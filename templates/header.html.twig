<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Mon site d'articles !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{path('homepage')}}">Accueil</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{path('liste_article')}}">Liste des articles</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{path('creation_article')}}">Créer un article</a>
            </li>            
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" id="compteDropdownLink">
                        <img src="{{app.user.avatar}}" class="avatar avatar-mini" alt="Avatar de {{app.user.FullName}}"> {{app.user.FullName}} {# Rajouter du style si trop gros #}
                    </a>

                    <div class="dropdown-menu dropdown-menu-rigth" aria-labelledby="compteDropdownLink">
                        <a href="{{path('creation_article')}}" class="dropdown-item">Créer un article</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{path('compte_myuser')}}" class="dropdown-item">Mon profil</a>
                        <a href="{{path('compte_profil')}}" class="dropdown-item">Modifier le profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{path('compte_logout')}}" class="dropdown-item">Déconnection</a>
                    </div>                    
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{path('compte_register')}}">S'inscrire</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{path('compte_login')}}">Se connecter</a>
                </li>        
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container mt-4">
        <div class="alert alert-{{label}}">
            {% for message in messages %}                        
                <p>{{message}}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}